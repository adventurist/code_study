#include <iostream>
#include <vector>
#include <unordered_map>


struct Edge
{
  int node;
};

using Graph = std::unordered_map<int, std::vector<Edge>>;

void DualDistance()
{
  int test_num{};
  Graph graph{};

  scanf("%d", &test_num);
  graph.reserve(test_num)
  for (int i = 0; i < test_num; i++)
  // scanf("%d %d", &b, );
}

int main(int argc, char* argv[])
{


  while (a <= b)
  {
    if (a % 2)
      std::cout << a << ' ';
    a++;
  }

  return 0;
}